import { Address } from "@solana/web3.js";
import { getMintEncoder, getTokenEncoder, getMintSize, getTokenSize, AccountState } from "@solana-program/token";
import * as ffi from "../ffi.js";
import { serializeInstructions, serializeAccounts } from "./wire.js";
import { deserializeResult } from "../internal/deserialize.js";
import { ExecutionResult } from "./result.js";
import { QuasarSvmBase, QUASAR_SVM_CONFIG_FULL } from "../base.js";
import { SPL_TOKEN_PROGRAM_ID, SPL_TOKEN_2022_PROGRAM_ID, SPL_ASSOCIATED_TOKEN_PROGRAM_ID, SYSTEM_PROGRAM_ID, LAMPORTS_PER_SOL, LOADER_V2, LOADER_V3, loadElf, } from "../programs.js";
import { rentMinimumBalance } from "../token.js";
export { ExecutionResult } from "./result.js";
export { QUASAR_SVM_CONFIG_FULL } from "../index.js";
export { SPL_TOKEN_PROGRAM_ID, SPL_TOKEN_2022_PROGRAM_ID, SPL_ASSOCIATED_TOKEN_PROGRAM_ID, LOADER_V2, LOADER_V3, LAMPORTS_PER_SOL } from "../programs.js";
export { AccountState } from "@solana-program/token";
const mintEncoder = getMintEncoder();
const tokenEncoder = getTokenEncoder();
// ---------------------------------------------------------------------------
// QuasarSvm
// ---------------------------------------------------------------------------
export class QuasarSvm extends QuasarSvmBase {
    constructor(config = QUASAR_SVM_CONFIG_FULL) {
        super();
        if (config.token)
            this.addProgram(new Address(SPL_TOKEN_PROGRAM_ID), loadElf("spl_token.so"), LOADER_V2);
        if (config.token2022)
            this.addProgram(new Address(SPL_TOKEN_2022_PROGRAM_ID), loadElf("spl_token_2022.so"), LOADER_V3);
        if (config.associatedToken)
            this.addProgram(new Address(SPL_ASSOCIATED_TOKEN_PROGRAM_ID), loadElf("spl_associated_token.so"), LOADER_V2);
    }
    addProgram(programId, elf, loaderVersion = LOADER_V3) {
        this.check(ffi.quasar_svm_add_program(this.ptr, programId.toBuffer(), Buffer.from(elf), elf.length, loaderVersion));
        return this;
    }
    // ---------- Execution ----------
    processInstruction(instruction, accounts) {
        return this.exec(ffi.quasar_svm_process_transaction, serializeInstructions([instruction]), serializeAccounts(accounts));
    }
    processInstructionChain(instructions, accounts) {
        return this.exec(ffi.quasar_svm_process_transaction, serializeInstructions(instructions), serializeAccounts(accounts));
    }
    // ---------- Internal ----------
    exec(fn, ixBuf, acctBuf) {
        return new ExecutionResult(deserializeResult(this.execRaw(fn, ixBuf, acctBuf)));
    }
}
// ---------------------------------------------------------------------------
// Helper — builds a KeyedAccountInfo
// ---------------------------------------------------------------------------
function keyed(addr, owner, lamports, data, executable = false) {
    return {
        accountId: addr,
        accountInfo: {
            owner,
            lamports,
            data: Buffer.isBuffer(data) ? data : Buffer.from(data),
            executable,
        },
    };
}
// ---------------------------------------------------------------------------
// Account factories
// ---------------------------------------------------------------------------
/** Create a system-owned account. Defaults to 1 SOL if lamports omitted. */
export function createKeyedSystemAccount(address, lamports = LAMPORTS_PER_SOL) {
    return keyed(address, new Address(SYSTEM_PROGRAM_ID), lamports, Buffer.alloc(0));
}
/** Create a pre-initialized mint account. */
export function createKeyedMintAccount(address, opts = {}, tokenProgramId = new Address(SPL_TOKEN_PROGRAM_ID)) {
    const data = Buffer.from(mintEncoder.encode({
        mintAuthority: opts.mintAuthority ? opts.mintAuthority.toBase58() : null,
        supply: opts.supply ?? 0n,
        decimals: opts.decimals ?? 9,
        isInitialized: true,
        freezeAuthority: opts.freezeAuthority ? opts.freezeAuthority.toBase58() : null,
    }));
    return keyed(address, tokenProgramId, rentMinimumBalance(getMintSize()), data);
}
/** Create a pre-initialized token account. */
export function createKeyedTokenAccount(address, opts, tokenProgramId = new Address(SPL_TOKEN_PROGRAM_ID)) {
    const data = Buffer.from(tokenEncoder.encode({
        mint: opts.mint.toBase58(),
        owner: opts.owner.toBase58(),
        amount: opts.amount,
        delegate: opts.delegate ? opts.delegate.toBase58() : null,
        state: (opts.state ?? AccountState.Initialized),
        isNative: opts.isNative ?? null,
        delegatedAmount: opts.delegatedAmount ?? 0n,
        closeAuthority: opts.closeAuthority ? opts.closeAuthority.toBase58() : null,
    }));
    return keyed(address, tokenProgramId, rentMinimumBalance(getTokenSize()), data);
}
/** Create a pre-initialized associated token account. Derives the ATA address automatically. */
export function createKeyedAssociatedTokenAccount(owner, mint, amount, tokenProgramId = new Address(SPL_TOKEN_PROGRAM_ID)) {
    const [ata] = Address.findProgramAddressSync([owner.toBuffer(), tokenProgramId.toBuffer(), mint.toBuffer()], new Address(SPL_ASSOCIATED_TOKEN_PROGRAM_ID));
    const data = Buffer.from(tokenEncoder.encode({
        mint: mint.toBase58(),
        owner: owner.toBase58(),
        amount,
        delegate: null,
        state: AccountState.Initialized,
        isNative: null,
        delegatedAmount: 0n,
        closeAuthority: null,
    }));
    return keyed(ata, tokenProgramId, rentMinimumBalance(getTokenSize()), data);
}
/** Generic account factory — encodes data with any @solana/codecs-compatible encoder. */
export function createKeyedAccount(address, owner, encoder, data, lamports) {
    const encoded = Buffer.from(encoder.encode(data));
    return keyed(address, owner, lamports ?? rentMinimumBalance(encoded.length), encoded);
}
//# sourceMappingURL=index.js.map